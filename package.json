{
  "name": "chimeric-avatar",
  "private": true,
  "packageManager": "pnpm@10.29.3",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "schema:check": "pnpm --filter @chimera/schema check"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
