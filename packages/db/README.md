# DB Package

Web実装のDB schemaとmigrationを管理する。

P0ではSupabase PostgreSQLを使い、最小テーブルは`docs/web-implementation-design.md`の`DB Minimum Schema`を正本にする。

実装対象:

```text
migrations/0001_lab_web_schema.sql
```

初期fixture:

```text
sample A0 poster session
sample paper text
sample beginner/master/professional scripts
runtime manifest fixture
```
